import { InfoheadDirective } from './infohead.directive';

describe('InfoheadDirective', () => {
  it('should create an instance', () => {
    const directive = new InfoheadDirective();
    expect(directive).toBeTruthy();
  });
});
